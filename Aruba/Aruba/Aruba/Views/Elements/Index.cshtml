@model ElementsViewModel
@{
    Layout = "_Layout";
    ViewData["Title"] = "Elements";
}

@section Scripts {
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
}

@Html.ActionLink("Reset", "Reset", "Elements")

<div class="row">
    <div class="col-md-6">
        <h1>Types</h1>
        <ul>
            @foreach (var element in Model.Elements)
            {
                <li>@element.Type</li>
            }
        </ul>

        <div class="card-columns">
            @foreach (var element in Model.Elements)
            {
                <div class="card text-center">
                    <div class="card-header">
                        <p><strong>@element.AtomicNumber </strong> @element.Name</p>
                        <h3>@element.Symbol</h3>
                    </div>
                    <div class="card-body">
                        <p>Price: @element.Price</p>
                        <p>@element.Description</p>
                    </div>
                    <div class="card-footer">
                        <span>@element.Type</span>
                    </div>
                </div>
            }
        </div>
    </div>

    <div class="col-md-6">
        @{await Html.RenderPartialAsync("_AddElement", new ElementViewModel()); }
    </div>
</div>

@Html.ActionLink("Invalid action to trigger error page", "UnKnownAction", "Elements", new { }, new { @class = "nav-link text-dark" })
